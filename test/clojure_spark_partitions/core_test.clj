(ns clojure-spark-partitions.core-test
  (:require [clojure.test :refer :all]
            [clojure-spark-partitions.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
